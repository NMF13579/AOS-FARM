# AOS Tools
AOS strictly relies on AI Agent instruction following. The tools provided here are simple shell scripts for dry-run validation. AOS does not use active python runners or CI hooks to enforce behavior.
